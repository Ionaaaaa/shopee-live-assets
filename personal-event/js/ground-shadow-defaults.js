/*
  ground-shadow-defaults.js
  地平線陰影的「形狀」參數（顏色不在這裡，顏色是 color-theme-engine.js 依背景色自動算的）。

  想幫某個版位加地平線效果：加一組、改對應的版位id即可；
  沒列出來的版位不會有這個效果（沒有 _default 是刻意的——這是視覺效果，
  不像顏色/縮放那樣每個版位都該有，沒特別調過形狀寧可不顯示，也不要套錯比例）。

  （2026-07：原本這裡有一組 02_lpbn 的地平線陰影參數，是從上一個專案沿用過來的
  效果，這個專案不需要，已經移除。layouts/02_lpbn.html 的 drawGroundShadow()
  會在找不到對應設定時自動跳過不畫，不用改繪製邏輯本身，之後如果這個專案
  真的需要地平線效果，比照舊格式加一組 '02_lpbn': {...} 回來即可。） */
window.GroundShadowDefaults = {

};
