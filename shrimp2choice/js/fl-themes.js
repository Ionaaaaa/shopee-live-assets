'use strict';
/* ══════════════════════════════════════════════════════════════════
   fl-themes.js — 直播間FL（05_fl）底色
   ----------------------------------------------------------------
   這個工單只有一種設計，沒有「公版款式」可以選（不像個人專場那種
   A/B/C多款視覺）。之前借用 mcn代播 那套 A/B/C/D/N 五色配色表只是
   暫時讓「切換公版」這個機制先動起來的權宜作法，現在拿掉、改成
   固定一個底色——editor 右側不會再出現公版挑選按鈕。

   文案顏色固定黃色 rgb(255,232,139)（案型字卡規格 MD 裡 05_fl 那份
   PSD 備註寫的固定值），不受這裡影響，所以這裡不重複定義文字色。

   ── 顏色值 ──
   #ee4d24（你先前指定的固定底色），如果之後要調整，改這裡的
   FL_FIXED_BG_COLOR 一個數字就好，不用動 layouts/05_fl.html。
   ══════════════════════════════════════════════════════════════════ */

var FL_FIXED_BG_COLOR = '#ee4d24';

function getFlThemeBoxColor(){
  return FL_FIXED_BG_COLOR;
}
